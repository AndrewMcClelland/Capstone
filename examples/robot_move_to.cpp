/* ***
	// Parse comand line
	if (argc >= 2) {
		if (sscanf(argv[1], " %lf", &destination.x) != 1) { // did not capture into &destination.x
			LOG <<  "main(): Invalid argument 1!" << endl;
			return false;
		}
	}
	if (argc >= 3) {
		if (sscanf(argv[2], " %lf", &destination.y) != 1) { // did not capture into &destination.y
			LOG <<  "main(): Invalid argument 2!" << endl;
			return false;
		}
	}
	if (argc >= 4) {
		if (sscanf(argv[3], " %lf", &destination.z) != 1) { // did not capture into &destination.a
			LOG <<  "main(): Invalid argument 3!" << endl;
			return false;
		}
	}
	if (argc >= 5) {
		if (sscanf(argv[4], " %lf", &destination.j4) != 1) { // did not capture into &destination.j4
			LOG <<  "main(): Invalid argument 4!" << endl;
			return false;
		}
	}
	if (argc >= 6) {
		if (sscanf(argv[5], " %lf", &destination.j5) != 1) { // did not capture into &destination.j5
			LOG <<  "main(): Invalid argument 5!" << endl;
			return false;
		}
	}
	if (argc >= 7) {
		if (sscanf(argv[6], " %lf", &destination.j6) != 1) { // did not capture into &destination.j6
			LOG <<  "main(): Invalid argument 6!" << endl;
			return false;
		}
	}
*** */
